#include "main.h"

/**
 * add - a function that adds two integers and returns the result.
 * @a: first argument
 * @b: second argument
 * Return: sum
 */
int add(int a, int b)
{
int sum;
sum = a + b;
return (sum);
}
