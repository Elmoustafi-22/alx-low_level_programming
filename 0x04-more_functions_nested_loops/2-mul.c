#include "main.h"

/**
 * mul - a function that multiplies two integers.
 * @a: first argument
 * @b: second agument
 * Return: result
 */
int mul(int a, int b)
{
	int result;
	result = a * b;
	
	return (result);
}
